apiVersion: v1
kind: Service
metadata:
  name: todolist-nodeport
  namespace: todoapp
spec:
  type: NodePort
  selector:
    app: todolist
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
