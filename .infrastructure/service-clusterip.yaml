apiVersion: v1
kind: Service
metadata:
  name: todolist-clusterip
  namespace: todoapp
spec:
  type: ClusterIP
  selector:
    app: todolist
  ports:
    - name: http
      port: 8080
      targetPort: 8080
